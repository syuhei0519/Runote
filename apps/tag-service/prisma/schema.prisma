generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tag {
  @@map("tag")
  id        Int      @id @default(autoincrement())
  name      String   @unique  // タグ名は重複禁止
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}